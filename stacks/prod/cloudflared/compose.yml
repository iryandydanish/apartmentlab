services:
  cloudflared:
    image: ${CLOUDFLARED_IMAGE}
    networks:
      - edge
    restart: unless-stopped
    command: ["tunnel", "--no-autoupdate", "run"]
    env_file:
      - /apartmentlab/gitops/env/prod/cloudflared.env

networks:
  edge:
    external: true
