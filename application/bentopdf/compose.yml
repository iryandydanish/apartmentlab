services:
  bentopdf:
    image: bentopdf/bentopdf:${BENTOPDF_TAG}
    restart: unless-stopped
    networks:
      - edge
    expose:
      - "8080"
    ports:
      - "3001:8080"
    profiles: ["preprod"]

networks:
  edge:
    external: true
